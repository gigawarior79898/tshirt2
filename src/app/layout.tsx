import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Fashion Hub - Premium Fashion Platform',
  description: 'Discover premium fashion collections in an immersive 3D experience',
  keywords: 'fashion, clothing, premium, 3D, shopping, style',
  authors: [{ name: 'Fashion Hub Team' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}